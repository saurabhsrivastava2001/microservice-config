server:
  port: 7912

spring:
  application:
    name: carrentals-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7910/eureka/

management:
  tracing:
    sampling:
      probability: 1.0
