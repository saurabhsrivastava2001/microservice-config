server:
  port: 7077

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userservice
    username: root
    password: Mahadev12@
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
